{
	"name": "AI Dev Base Image Development",
	"image": "ghcr.io/dataviking-tech/ai-dev-base:v2",
	//"build": {"dockerfile": "../Dockerfile"},

	"features": {
		"ghcr.io/devcontainers/features/docker-in-docker:2": {
			"version": "latest",
			"dockerDashComposeVersion": "v2"
		}
	},

	"customizations": {
		"vscode": {
			"extensions": [
				"ms-azuretools.vscode-docker"
			]
		}
	},

	"mounts": [
		"source=/var/run/docker.sock,target=/var/run/docker.sock,type=bind"
	],

	"postCreateCommand": "echo 'Using ai-dev-base:v2 to develop next version'",

	"remoteUser": "vscode"
}
